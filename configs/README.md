# Config Notes

- All configs are locked to `scale = 2`.
- Update dataset roots before running train/eval.
- `temporal_small.toml` is the default fast-cycle config.
- `temporal_full.toml` is the full-budget config for final reporting.
